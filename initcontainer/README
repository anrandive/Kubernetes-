kubectl.exe create secret generic gitsecret --from-file known_hosts=$HOME\.ssh\known_hosts --from-file ssh=$HOME\.ssh\id_rsa

#kubectl.exe create secret generic knownhosts --from-file $HOME\.ssh\known_hosts
#kubectl.exe create secret generic git-creds --from-file ssh=$HOME\.ssh\id_rsa
